services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-usedcars}
      POSTGRES_USER: ${POSTGRES_USER:-usedcars}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-usedcars}
    ports:
      - "5442:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - usedcar_net

volumes:
  postgres_data:

networks:
  usedcar_net:
    driver: bridge